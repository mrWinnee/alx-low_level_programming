#include "main.h"
/**
 * _abs - give the absolute value of a number.
 * @n: The int to give it's absolute.
 *
 * Return: the absolute value of n.
 */
int _abs(int n)
{
if (n < 0)
return (n * -1);

return (n);
}

