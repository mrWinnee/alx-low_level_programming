#include <stdio.h>
#include "main.h"
/**
 * main - checks the code.
 * @argc: the size of the argv[].
 * @argv: array of the cl arrguments.
 * Return: always 0.
 */

int main(int argc, char *argv[])
{
	printf("%s\n", argv[argc * 0]);
	return (0);
}

